# 最初のゴール: 日々の支出管理

まず本人1人がスマホから利用する。家族との共有は別段階とする。

## 完成条件

- メール認証でログインし、famFi の利用許可を持つ本人だけが使える。
- 円単位の正の整数、日付、カテゴリで支出を記録する。内容とメモは任意。
- 月別の一覧、カテゴリ別内訳、合計、編集、削除が実データで動く。
- 再読み込み・別端末でも記録が残る。
- 未ログイン、別ユーザー、別アプリからの取得・変更を拒否する。
- CSV出力と、バックアップ・復旧の手順を用意する。
- 本番とスマホで確認し、サンプル金額を本物として表示しない。

## 優先度

1. 認証、利用許可、共有DB内の専用テーブル・権限。
2. 支出CRUDと永続化。二重保存と編集競合の防止。
3. スマホ入力、月別集計、カテゴリ絞り込み、CSV出力。
4. 権限・操作テスト、バックアップ復元確認、本番反映。
5. 利用開始後: 入力テンプレート、固定費、カテゴリ管理。
6. 後続: 収入、予算、家族共有、CSV取込、詳細分析。

レシピ、家事、予定などの試作コードは残すが、最初の本番版には公開しない。
共有DBの正本は `personal-apps-infra`。実施済みの状態は同リポジトリと
`docs/shared-database.md` に記録する。

## 2026-09-09 の進捗

- 実装済み: メールコード認証、本人用の利用許可、専用ロールとRLS、支出CRUD、再送の二重保存防止、編集競合の検出。
- 実装済み: スマホ・PC画面、月別合計、カテゴリ内訳・絞り込み、CSV出力、暗号化バックアップのCLI。
- 検証済み: 単体テスト、ローカルPostgresの権限・CRUD・暗号化バックアップ復元、実APIを通す架空Authの統合テスト、実Supabase接続ロールの拒否テスト、スマホ・PC操作。
- 未完了: 本人メールアドレス確定、Authアカウント・membershipの登録、メール配信設定、実メールでのログイン。
- 未完了: 本番で最初の支出を登録し、再ログイン・別端末で確認。実データの初回バックアップと別保存先の確保。
- 未完了: バックアップの自動実行。現在は手動CLIのみ。

最初の目標の達成判定は、上記の本番利用確認後に行います。認証未設定の間は準備中として公開します。
